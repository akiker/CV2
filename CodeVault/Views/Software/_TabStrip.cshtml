
@(Html.Kendo().TabStrip()
.Name("tabstrip")
.SelectedIndex(0)
.Items(items =>
{
    items.Add()
        .Text("Overview")
        .Content(@Html.Partial("_Overview").ToHtmlString());
    items.Add()
        .Text("Permissions")
        .Content(@Html.Partial("_Permissions").ToHtmlString());
})
)